# Django
DJANGO_SECRET_KEY=replace-with-a-long-random-secret
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost
DJANGO_LANGUAGE_CODE=en
DJANGO_TIME_ZONE=UTC
DJANGO_LOG_LEVEL=INFO

# PostgreSQL
DB_NAME=QR
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=127.0.0.1
DB_PORT=5432
DB_CONN_MAX_AGE=60
DB_SSLMODE=prefer

# Frontend / CORS
PUBLIC_WEB_BASE_URL=http://localhost:3000
CORS_ALLOWED_ORIGINS=http://127.0.0.1:3000,http://localhost:3000

# Auth
GOOGLE_CLIENT_ID=replace-with-google-oauth-client-id.apps.googleusercontent.com
JWT_ACCESS_MINUTES=15
JWT_REFRESH_DAYS=30
JWT_REFRESH_COOKIE_NAME=qr_refresh
JWT_REFRESH_COOKIE_SECURE=False
JWT_REFRESH_COOKIE_SAMESITE=Lax
# JWT_REFRESH_COOKIE_DOMAIN=.example.com

# Media storage. Leave S3_BUCKET_NAME empty in development to use MEDIA_ROOT.
# In production use a dedicated media bucket plus public CDN/custom domain so
# published site snapshots contain stable non-expiring asset URLs.
S3_BUCKET_NAME=
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_ENDPOINT_URL=
S3_REGION_NAME=
S3_CUSTOM_DOMAIN=
S3_QUERYSTRING_AUTH=False

# Privacy-safe analytics. Use a value distinct from DJANGO_SECRET_KEY in production.
ANALYTICS_HASH_SALT=replace-with-a-separate-random-secret

# Throttling
THROTTLE_ANON=600/hour
THROTTLE_GUEST_CREATE=20/hour
THROTTLE_GUEST_UPDATE=60/hour
THROTTLE_SLUG_CHECK=120/min
THROTTLE_UPLOAD_MEDIA=30/hour
THROTTLE_PUBLIC_READ=1200/hour
THROTTLE_ANALYTICS_WRITE=600/hour
THROTTLE_QR_REDIRECT=1200/hour
THROTTLE_AUTH=60/hour

# Production security overrides (defaults become secure when DJANGO_DEBUG=False)
SECURE_SSL_REDIRECT=False
SESSION_COOKIE_SECURE=False
CSRF_COOKIE_SECURE=False
SECURE_HSTS_SECONDS=0
